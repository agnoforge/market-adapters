// Package domain holds the market data acquisition vocabulary — Provider,
// Symbol, Timeframe, Dataset, Bar, Coverage, Gap and Trading Calendar — plus
// the invariants that hold regardless of which Provider or Store is in play.
//
// It depends on the standard library only.
package domain

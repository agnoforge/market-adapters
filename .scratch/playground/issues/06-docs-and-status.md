Status: done
Blocked by: 05

# Docs and status

README section "Playground" (open `/playground/`, what the trace shows), `ASSUMPTIONS.md` §09 for the playground JSON shapes and `X-Trace-ID`, update `where-we-are-at.md`.

## Done when
- [x] README links `/playground/` and ADR 0003
- [x] `ASSUMPTIONS.md` records trace JSON shape, header name, retention
- [x] `where-we-are-at.md` updated
